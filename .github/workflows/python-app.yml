# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python application

on:
  push:
    branches: [develop] #Antes lo tenía en main pero mi tutor me ha dicho que no toque esa rama

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.10
      uses: actions/setup-python@v2
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        python3 setup.py build
        python3 setup.py install
        python3 -c "import procesSource"
#        python3 -c "import graphSource"

    - name: Should Pass
      run: echo "Pasa"
    - name: Test with pytest
      run: python3 procesSource/tests/TestProcesador.py #La_donacion proiektua sortzen eta testeatzen du